# Terraform / OpenTofu state
.terraform/
*.tfstate
*.tfstate.backup
.terraform.lock.hcl

# Secret variables files
# These should not be committed to Git. A terraform.tfvars.example should be used instead.
terraform.tfvars
*.tfvars
pulumi-token

# IDE and editor files
.idea/
.vscode/
*.swp
*.swo

# macOS files
.DS_Store

# Windows files
Thumbs.db

# Log files
*.log

# Istio downloads
istio-*/

# Troubleshooting output files
troubleshoot/

*.vagrant/
.vagrant/

# Kubernetes secrets and sensitive files
*-secret.yaml
*.key
*.pem
*.crt
kubeconfig
*.kubeconfig

# But allow External Secrets CRDs (these don't contain actual secrets)
!platform/identity/*/external-secrets.yaml
!platform/identity/*/secretstore.yaml
!platform/devtools/*/external-secrets.yaml
!platform/devtools/*/secretstore.yaml
!platform/external-secrets/*.yaml

# Backup files
*-backup.yaml
*.backup
*~

# Environment files with secrets
.env
.env.local
.env.*.local

# Helm values with secrets (use *-values-secret.yaml pattern)
*-values-secret.yaml
*-secret-values.yamlplatform/networking/tailscale/oauth-secret.yaml
