apiVersion: v1
kind: ResourceQuota
metadata:
  name: compute-quota
  namespace: observability
spec:
  hard:
    requests.cpu: "10"
    requests.memory: 16Gi
    limits.cpu: "21"
    limits.memory: 32Gi
    persistentvolumeclaims: "12"
---
apiVersion: v1
kind: LimitRange
metadata:
  name: mem-limit-range
  namespace: observability
spec:
  limits:
    - default:
        memory: "1Gi"
        cpu: "500m"
      defaultRequest:
        memory: "256Mi"
        cpu: "200m"
      type: Container
