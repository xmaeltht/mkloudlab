# Application manifest for Keycloak Gateway and Certificate
apiVersion: argoproj.io/v1alpha1
kind: Application
metadata:
  name: keycloak-gateway
  namespace: argocd
  finalizers:
    - resources-finalizer.argocd.argoproj.io
spec:
  project: default
  source:
    repoURL: "https://github.com/xmaeltht/mkloudlab.git"
    path: services/keycloak
    targetRevision: HEAD
  destination:
    server: "https://kubernetes.default.svc"
    namespace: keycloak
  syncPolicy:
    automated:
      prune: true
      selfHeal: true
    syncOptions:
      - CreateNamespace=true
